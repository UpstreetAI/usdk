import { Meta, Title, IconGallery, IconItem } from '@storybook/blocks';
import * as Icons from '../../icons';

<Meta title="Elemets/Icons" />

# Icons
## {Object.entries(Icons).length} icons available:
# 

<IconGallery>
{Object.entries(Icons)
.filter(([name]) => name !== "iconList")
.map(([name, Icon]) => (
  <IconItem name={name}>
    <Icon />
  </IconItem>
))
}
</IconGallery>