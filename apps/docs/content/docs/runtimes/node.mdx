---
title: Node
description: Run your agent using the Node.js runtime (default)
full: false
---

import Wrapper from 'components/preview/wrapper';
import Image from 'next/image';

# Node Runtime

The Node.js runtime is the default runtime for running agents. When you use the `usdk chat` command without specifying a runtime, it will use Node.js.

However, you can also specify this explicitly:

```bash
usdk chat --runtime node
```