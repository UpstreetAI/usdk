---
title: Electron
description: Run the agent on your desktop.
full: false
---

import Wrapper from 'components/preview/wrapper';
import Image from 'next/image';

# Electron Runtime

The Electron runtime allows you to run your agent as a desktop application.

## Usage

To run your agent in Electron, use the `usdk chat` command with the `--runtime` flag set to `electron`:

```bash
usdk chat --runtime electron
```

...Coming soon...
