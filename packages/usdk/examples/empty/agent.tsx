import React from 'react';
import {
  RawAgent,
/* IMPORTS REGEX HOOK */
} from 'react-agents';

export default function MyAgent() {
  return (
    <RawAgent>
{/* JSX REGEX HOOK */}
    </RawAgent>
  );
}
